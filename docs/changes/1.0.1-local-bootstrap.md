# 1.0.1 — Local bootstrap (in progress)

## What

- Branch: `feature/loki-production-hardening`
- Local Docker Postgres via `docker-compose.dev.yml` (host port **5434** → container 5432)
- Invented `server/.env` from `.env.example` (no teammate secrets required)
- Changelog + agent documentation rules

## Verify

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db
npm install
npm install --prefix server
npm run db:migrate
npm run db:seed
npm run dev:server   # :3001
npm run dev          # :5173
```

Login: `admin@transitix.ro` / `admin123` or `sofer@transitix.ro` / `sofer123`

Note: host port is **5434** because 5432/5433 were already taken by other local containers.
