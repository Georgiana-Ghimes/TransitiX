# 1.8.1 — Avize export actor, split page, route rules

Versiune: **1.8.1** (sidebar, login, tab, `/api/health`).

Ce s-a închis din Faza 2 / P1–P3 **fără** porți RAI și **fără** ANAF/GPS.

## Ce s-a schimbat

- `aviz_export_log.user_id` + coloana **Cine** în Rapoarte
- `AvizeReports.jsx` orchestrează tab-uri din `src/pages/avize/`
- Ciornă factură și lock Anexa RAI testate unitar
- Upload rate limit 60/min/firmă
- Toast la eșec listă (flotă, șoferi, clienți, documente, dashboard)
- Avize cu `trip_id` pe `/trips/:id`
- `server/.env.example` pe host **5434** (Docker); comentariu pentru Postgres nativ pe 5432

## Cum verifici

1. Refresh — sidebar/tab arată `v1.8.1`
2. `npm run db:migrate` (coloana `user_id` pe export log)
3. Unește o anexă, tab Rapoarte → rândul are numele tău
4. Cursă cu aviz legat → secțiunea Avize legate
5. `npm test` + `npm run lint`

Nu s-a schimbat: TPO duplicat (avertizează, salvează), rută Excel, e-Factura SPV, GPS live.
