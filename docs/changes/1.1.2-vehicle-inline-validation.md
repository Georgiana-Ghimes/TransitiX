# 1.1.2 — Vehicle form inline validation

## Problem

Huge integers (e.g. `3213213213`) in Capacitate / Kilometraj / An caused a 500 (`integer out of range`). Toast errors sat *under* the modal on mobile.

## Change

- [`src/components/VehicleForm.jsx`](../../src/components/VehicleForm.jsx): client-side limits (INT max, year range, consum ≤ 999.99), red field + message, in-form banner
- [`src/components/FormFeedback.jsx`](../../src/components/FormFeedback.jsx): reusable banner / field error
- Toast viewport `z-[200]` above ModalShell `z-[100]`

## Verify

1. Flotă → Vehicul nou
2. Put `3213213213` in Capacitate (kg) → red field + banner, no 500
3. Consum `10000` → “max. 999.99”
4. Valid save still works
