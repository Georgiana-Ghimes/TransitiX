# 1.1.3 — Notification panel + local Vite proxy

## Fixed

- Mobile notification dropdown no longer clips off the left edge (viewport-fixed under the header on small screens)
- Vite `server.host` / proxy target set to `127.0.0.1` so Windows IPv6 `localhost` does not break `/api` login

## Verify

1. Phone viewport → open bell → full “Notificări” panel visible
2. Open http://127.0.0.1:5173 → login `admin@transitix.ro` / `admin123`
