# 1.2.0 — Production increments (settings, email, harden)

## What

Closes the remaining option-B chunks from local bootstrap:

1. **README** — product map, stubs, Docker 5434, seed logins, no teammate secrets
2. **Settings** — `GET/PUT /api/company` + live Setări form; Documente uses expiry-day thresholds
3. **Email** — Resend if env is set; otherwise console stub (password reset still returns a local link)
4. **Harden / ops** — refresh tokens (company-bound, client 401 retry), login rate limit, office-only entity delete, admin-only company writes, health+db, JWT_SECRET required, unused Stripe/dnd deps removed

## Verify

```bash
npm test
npm run lint
npm run build
```

1. Log in as admin → Setări → change company name / expiry checkboxes → Salvează → reload
2. Documente subtitle uses the max selected threshold
3. Without Resend: Finance “send invoice” still succeeds (stub); forgot-password shows a local link
4. `GET http://127.0.0.1:3001/api/health` → `{ ok, db: true }`
