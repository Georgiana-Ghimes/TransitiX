# 1.7.0 — Avize increments A–E (on RAI-spedition)

Work landed on **`RAI-spedition`**, not a feature branch. Client PDFs stay local (`server/uploads/` is gitignored).

## What

- **A:** street aliases (Blvd/Bld/Aleea/Al./Pța), `extraction_source` badge, list filters (`from`/`to`/`status`/`q`, limit 200), duplicate TPO warning (save allowed, no UNIQUE), file preview in Editează, nullable `ruta_display` (Excel still Client → Livrare).
- **B:** Bucharest date presets, bulk confirm, per-company observation codes, email annex (Resend or stub + download), zip XLSX + original files. PDF split skipped (RAI did not ask).
- **C:** Vision calls live in `avizVision.js`; text-layer remains default; field confidence highlight; VM key runbook in `docs/ops-google-vision.md` (no secrets).
- **D:** optional `trip_id` + suggestions; draft invoice from confirmed avize; no ANAF/e-Factura.
- **E:** Rapoarte tab (km/plate, weekly counts, export history). Default **Anexa Factura RAI** cannot be overwritten.

## Verify

```bash
npm run db:migrate
npm test
npm run lint
```

1. Stay on `RAI-spedition`. Restart API after migrate.
2. Avize: filter by week, upload a text PDF → badge **Text PDF**.
3. Editează: preview, set **Rută birou**, save, Re-extrage — alias remains; Excel still uses `ruta_transport`.
4. Save a second row with the same TPO — warning, both rows exist.
5. Confirm selected, Unește, Zip, Email (stub downloads if Resend is unset).
6. Rapoarte tab lists km/plate; Șabloane cannot edit locked Anexa Factura RAI.
7. Ciornă factură from confirmed rows appears in Financiar as `draft` / `not_sent`.
